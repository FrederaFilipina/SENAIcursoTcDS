# Projeto: `Clínica BackEnd`

## Parte: #04

### ▷ Adicionando Hash de cripitografia
1. No terminal da pasta do projeto, execute os comandos:
    1. npm install bcrypt
    2. npm install --save-dev @types/bcrypt

2. Criar o arquivo `hash.ts` na pasta `src`
    1. Fazer a importação bcrypt
    2. Criar variável de senha


### ▷ Criando o Token
npm install jsonwebtoken
npm install -D @types/jsonwebtoken

criar arquivo token.ts na pasta src


### ▷ criar pasta utils dentro de src
criar arquivo createHash.ts
